# xdg-desktop-portal-hyprland.
#
# allow_token_by_default lets a screencast request reuse a previously granted
# token instead of reopening the source picker every single time. Without it,
# every share in Discord, TeamSpeak or OBS re-prompts.

screencopy {
    allow_token_by_default = true
}
