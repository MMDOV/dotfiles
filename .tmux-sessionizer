#!/usr/bin/env bash

tmux new-window -dn python
tmux send-keys -t python "cd $HOME/dev/pyprojects/ && clear" C-m
tmux new-window -dn ping "ping 8.8.8.8"
tmux new-window -dn yazi "yazi"
clear
fastfetch
